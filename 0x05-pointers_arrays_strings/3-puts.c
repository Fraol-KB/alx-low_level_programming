#include "main.h"

/**
 * _puts - Prints String
 * @str: string pointer
 * Return: none.
 */
void _puts(char *str)
{
	int a;

	a = 0;
	while (*(s + a))
	{
		_putchar(*(s + a));
		a++;
	}
	_putchar('\n');
}
